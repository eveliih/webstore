{
  "name": "webstore",
  "version": "1.0.0",
  "description": "backend for webstore project",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "cd ./webstore/ && npm install && npm run build  && copy .\\web.config .\\dist && xcopy /I/E dist ..\\dist",
    "build:ui:ubuntu": "rm -rf dist && cd ./webstore/ && npm install && npm run build && cp ./web.config ./dist && cp -r dist ../",
    "test": "echo \" no test specified\" "
  },
  "author": "Eveliina",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "sequelize": "^6.37.1",
    "tedious": "^18.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}

{
  "name": "webstore",
  "version": "1.0.0",
  "description": "backend for webstore project",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "if exist dist rd /s/q dist && cd ./webstore/ && npm run build && xcopy /I/E dist ..\\dist",
    "build:ubuntu:ui": "rm -rf dist && cd ../webstore/ && npm run build && cp -r dist ..",
    "test": "echo \" no test specified\" "
  },
  "author": "Eveliina",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
